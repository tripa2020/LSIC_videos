# environment
.env
.venv/
__pycache__/
*.pyc
.DS_Store

# pipeline runtime artifacts
work/

# huge source binaries — not tracked
LSIC_Downloads/

# external tooling — separate concern from this project
clai/

# stale nested duplicate clone (zero unique commits — already in this repo's history)
LSIC_videos/

# IDE
.idea/
.vscode/

# scratch scrape (regenerable via download_lsic/harvest.py)
download_lsic/recon_artifacts/

# workspace agent tooling (skills/hooks/archive/personal config) — not project code
.claude/
.coverage
