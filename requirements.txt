pydantic>=2.7,<3
python-dotenv>=1.0

# stage 2 onward — installed at M0 but not imported until later milestones
google-genai>=0.5
python-pptx>=1.0
PyMuPDF>=1.24       # replaces pdf2image+poppler — pure-pip, no system deps
reportlab>=4.0      # slide_book._render_pdf — pure-pip PDF generation (was an undeclared dep)
scenedetect>=0.6
opencv-python>=4.10
imagehash>=4.3
Pillow>=10.0

# acquisition front-end (download_lsic)
yt-dlp>=2025.1

# dev / test
pytest>=8.0
