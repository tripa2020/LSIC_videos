"""Stage 4: sectioning + per-deck TF-IDF fingerprint match for presentation windows. Lands in M4."""


def align(*args, **kwargs):
    raise NotImplementedError("align lands in M4 — see PLAN.md")


def match_presentations(*args, **kwargs):
    raise NotImplementedError("match_presentations lands in M4 — see PLAN.md")
