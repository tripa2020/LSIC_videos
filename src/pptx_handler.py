"""PPTX text + speaker notes via python-pptx; slide PNG render via libreoffice. Lands in M1."""


def extract(*args, **kwargs):
    raise NotImplementedError("pptx_handler.extract lands in M1 — see PLAN.md")
